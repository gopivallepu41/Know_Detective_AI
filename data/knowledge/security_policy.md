# Security Policy

All authentication tokens must use the current JWT v2 signing configuration. Production runbooks should describe the current signing key and token expiry policy. Legacy authentication instructions must be removed after migration is complete.
